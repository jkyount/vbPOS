<Window x:Class="Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:POS1"
        x:Name="winLogin"
        mc:Ignorable="d" Height="750" Width="1199" Background="#FFFFC357" ResizeMode="NoResize" WindowState="Maximized" WindowStartupLocation="CenterScreen" ScrollViewer.VerticalScrollBarVisibility="Disabled" WindowStyle="None">
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:LoginViewBaseViewModel}">
            <local:LoginViewBase/>
        </DataTemplate>
    </Window.Resources>
  
    <Grid>
        <Frame Content="{Binding View.ViewModel}">
        
        </Frame>

        <Button Height="100" Width="200" x:Name="btn" Click="btn_Click" Content="TEST"/>
    </Grid>


</Window>
